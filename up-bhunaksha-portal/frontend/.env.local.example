# Public — embedded in the bundle. RESTRICT THIS KEY by HTTP referrer + API in
# Google Cloud Console before going to production. See README.md.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# Where the backend lives. Default is the local dev backend.
NEXT_PUBLIC_API_BASE=http://localhost:4000
